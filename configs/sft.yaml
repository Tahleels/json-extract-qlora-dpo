# configs/sft.yaml
lora:
  r: 16
  alpha: 32
  dropout: 0.05
  target_modules: ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]

training:
  epochs: 3
  batch_size: 4
  grad_accum: 4
  learning_rate: 2e-4
  max_seq_length: 512
  output_dir: "artifacts/adapter_sft"
